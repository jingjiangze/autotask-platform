# -*- coding: utf-8 -*-
"""Test package marker.

Required so pytest imports test modules as ``tests.*`` and inserts the
repository root (not ``tests/``) on sys.path — otherwise this directory
would shadow the top-level ``cloud_test`` package.
"""
